rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Publicly readable
      allow read: if true;
      
      // Only authenticated users (admins) can write
      allow write: if request.auth != null;
    }
  }
}
